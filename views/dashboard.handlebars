<!-- This template includes hardcoded data. In future lessons, you will be rendering data dynamically. -->

<div class="menu-container">
  <div class="card-header col-md-10">
    <h2>
      📖 Read YOUR Latest Blog Posts... 🍩, you are logged in as {{username}} and your unique id is {{userID}}
    </h2>
  </div>

<button href="/newPost">Add a new post</button>

{{!-- display user's current posts down here --}}

{{!-- <div class="card text-white bg-info w-50 mx-auto border-secondary m-3 ">
  <div class="card-body">
    <h5 class="card-title">{{post.post_title}}</h5>
    <p class="card-text">{{post.post_content}}</p>
    <a href="/newcomment" class="btn btn-light" id="post-comment">Add a comment</a>
  </div>
</div>

{{#each post.comments}}
<div class="card text-white bg-secondary w-50 mx-auto border-secondary m-3 ">
  <div class="card-body">
    <h5 class="card-title">{{this.user_id}}</h5>
    <p class="card-text">{{this.comment_content}}</p>
  </div>
</div>
{{/each}} --}}

<script src="/js/post.js"></script>